const multiplicacao = function (num1:number, num2: number): number {
    return num1*num2;
} 
console.log(multiplicacao(2,13));

const saudacao = function (nome: string): string{
    return `Olá ${nome}`;
}
console.log(saudacao("Fulano"));
